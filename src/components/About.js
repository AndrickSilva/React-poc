import React from 'react'

const About = () => {
    return (
        <div className='container-fluid bg-light' style={{minHeight: "100vh"}}>
            <p className='container text-justify fw-bold fs-3'><span className="grad fs-1">CRUD</span> refers to the four basic operations a software application should be able to perform –<span className="grad "> Create, Read, Update, and Delete.</span> In such apps, users must be able to create data, have access to the data in the<span className="grad"> UI by reading the data, update or edit the data, and delete the data.</span></p>
        </div>
    )
}

export default About